select * from lankacraftdb1.category;

SELECT * FROM product WHERE category_id = (SELECT category_id FROM category WHERE name = 'Wood');
SELECT * 
FROM product 
WHERE category_id = (
    SELECT category_id FROM category WHERE name = 'Wood'
);
USE lankacraftdb1;
SELECT * 
FROM product  
WHERE category_id = (
    SELECT category_id FROM category WHERE name = 'Statues'
);
